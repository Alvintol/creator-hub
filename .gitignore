# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.*
*.local
!.env.example

# Scratch files used when writing secrets to disk to avoid shell escaping
# issues (e.g. gcloud secrets create --data-file=s.tmp) -- never commit these.
*.tmp

# Local Claude Code / Cowork output, not part of the shipped app
Claude outputs/
claude/
AGENTS.md

# Supabase CLI scratch
supabase/.temp/